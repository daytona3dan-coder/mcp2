# RECONSTRUCT

To reconstruct a decision preserve:

- canonical grant records as-of verification
- request facts
- verification timestamp
- replay-state fact relevant to the nonce
- verifier version
- resulting decision and fingerprints

Reconstruction reruns the deterministic verifier against those preserved inputs and compares the reproduced decision/fingerprints to the receipt.
